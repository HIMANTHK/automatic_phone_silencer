buildscript {
    repositories {
        google()
        mavenCentral() // Add this line
        jcenter() // You can remove this or leave it as a fallback
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.2'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.5.10"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral() // Add this line
        jcenter() // You can remove this or leave it as a fallback
    }
}
